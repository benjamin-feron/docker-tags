name: Docker build & push

on:
  create:
    tags:
      - '*.*.*'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
       name: Docker meta
       id: meta
       uses: docker/metadata-action@v3
       with:
         images: benjaminferon/docker-tags
         tags: |
           type=ref,event=tag
           type=semver,pattern={{version}}
           type=semver,pattern={{major}}.{{minor}}
           type=semver,pattern={{major}}
         labels: |
           org.opencontainers.image.title=docker-tags
           org.opencontainers.image.description=Retrieve tag list of a Docker repository
           org.opencontainers.image.url=https://github.com/benjamin-feron/docker-tags
           org.opencontainers.image.source=https://github.com/benjamin-feron/docker-tags
           org.opencontainers.image.licenses=GPLv3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to DockerHub
        uses: docker/login-action@v1 
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: .
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
